# Example env for local development only (do NOT commit real secrets)
# Copy to .env and fill locally. Production uses Secret Manager.

# Optional: override MCP URL for local testing
# MCP_URL=https://your-mcp-url.example/

# Host AI model (optional)
# HOST_MODEL=gemini-2.5-flash

# Never put real API keys in git. Use Secret Manager in prod.
# HOST_GOOGLE_API_KEY=REPLACE_ME_LOCALLY

# Optional allowlist of extension API keys (comma-separated)
# extension_api_keys=KEY1,KEY2
